$(document).ready(function() {
/*

//	if ($.browser.msie && $.browser.version == 9)
//		$('header, button, .button, input[type="submit"], input[type="reset"], input[type="button"]').addClass('gradient');


	$('#mobileselect').mobileMenu({
		switchWidth: 600, 
		prependTo: '.header-bg .two-thirds'
	});

	if ($('.container').width() > 959)
		$('.box').setBoxHeight();
	$('footer .column').setBoxHeight();

	$('#projects .img-box').imageSlide(-122);
	$('.portfolio-item').imageSlide(-158);

	// IE8
	if ($.browser.msie && $.browser.version == 8) {
		$.getScript("js/PIE.js", function () {
			$("header, .menu-page a, .button, .page-bar").each(function() { PIE.attach(this); });
		});
	}

	$(window).resize(function() {
		if ($(window).width() > 768) {
			$('.box').setBoxHeight();
			$('footer .column').setBoxHeight();
		} else {
			$('.box').height('auto');
			$('footer .column').height('auto');
		}
	});

	$.fn.imageSlide = function(margin) {
		$(this).hover(function() {
			$(this).find('img:first').animate({marginTop:margin + 'px'}, 300);
		}, function() {
			$(this).find('img:first').animate({marginTop:'0px'}, 300);
		});
	}

	$.fn.setBoxHeight = function() {
		var maxHeight = 0;
		$(this).each(function() {
			var boxheight = $(this).height();
			if (boxheight > maxHeight)
				maxHeight = boxheight; 
		});
		$(this).height(maxHeight);
	}

	$.detectMobile = function() {
		if( /Android|webOS|iPhone|iPad|iPod|BlackBerry/i.test(navigator.userAgent))
			return true;
		return false;
	}
*/

//	$('.fancybox').fancybox();
});
